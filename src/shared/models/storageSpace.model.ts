import {Address} from 'shared/models/address.model';

export class StorageSpace {
  id: number = 0;
  address: Address = new Address();
  name: string = '';
  tip: String;
  countyCode: String;
  city: String;
  flatNo: String;
  street: string;
  city: string;
  building: string;
  stair: string;
  floor: string;
  flatNo: string;
}
